[project]
name = "podcast_opml_watcher"
version = "0.1.0"
description = "Watch OPML file for new podcast episodes and transcribe them using podcast2md"
readme = "README.md"
authors = [
    { name = "Peter Reuterås", email = "peter@reuteras.net" }
]
requires-python = ">=3.12"
dependencies = [
    "feedparser>=6.0.13",
    "requests>=2.34.2",
]

[project.scripts]
podcast-opml-watcher = "podcast_opml_watcher:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "ruff>=0.16.0",
]
